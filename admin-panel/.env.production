# Build de PRODUCCIÓN (cuando el backend sirve el panel en /panel).
# Mismo origen → sin CORS. Sin emulador.
VITE_API_URL=
VITE_USE_EMULATOR=false
# Los VITE_FIREBASE_* reales se completan acá (o se pasan como build args en el Docker).
